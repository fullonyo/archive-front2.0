/**
 * Pagination Constants
 * Centralized configuration for infinite scroll and pagination
 */

/**
 * Default items per page for asset listings
 */
export const ASSETS_PER_PAGE = 15;

/**
 * Maximum pages to load (prevent infinite scroll issues)
 */
export const MAX_PAGES = 20;

/**
 * Scroll threshold for "scroll to top" button (in pixels)
 */
export const SCROLL_TOP_THRESHOLD = 400;

/**
 * Intersection Observer threshold for infinite scroll trigger
 */
export const INFINITE_SCROLL_THRESHOLD = 0.1;

/**
 * Default page number
 */
export const DEFAULT_PAGE = 1;
